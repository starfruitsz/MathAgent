# D 题进度看板

最后更新：**P10b 已完成**（四问数值全量重建 + Q4 中继归属修复 + Q1 ρ 扫描同口径） | 当前 HEAD：`<见 git log>`

> **接手步骤**：`git pull --rebase origin main` → **读 [`AGENT_GUIDE.md`](AGENT_GUIDE.md)**（四问位置索引 + 答案 + 思维链 + 初稿须知）→ 读本文件 → 读 `OPS_SPEC_D题.md`

---

## 阶段状态

- [x] **P0 数据发现**（附件已到位，全部字段结构实测完成）
- [x] **P0b 航段预计算**（`leg_cache.parquet`，240 条有序航段，哈希索引，构建 0.2 s）
- [x] **P1** Q1 单点往返最大安全载荷与货箱组批 —— **18 架次（B×9 + C×9）/ 59.087498 kWh / 32676.33 s**，
      精确字典序 DP（`exact_pack.py`），逐区达到架次数下界（下界合计 **18**、gap **0**），**架次数维度可证最优**；
      ★ ρ 扫描同口径：ρ=0.10 与 0.20 均为该值，ρ=0.30 → 20 架次 / 67.180487 kWh，ρ ≥ 0.40 无解
- [x] **P2** Q2 异构无人机多点多架次运输调度 —— **23 架次（A6+B9+C8）/ 66.789867 kWh / 完工 7843.2266 s（2.179 h）**；
      **首批 30/30、期望 80/80 全部达标**；**校验器 0 违规**；调度由 `dispatcher.py` 的 CP-SAT 区间调度求得（OPTIMAL）
- [x] **P3** Q3 通信约束下的运输与中继联合调度 —— **3 个中继架次** / 总能耗 **71.095918 kWh** / 联合完工 **7939.2924 s**；
      0.25 s 采样诊断：**19/23 架次零中断、928 个中断样本（0.929%）**，缺口按**中继资源限制**如实上报（`timeline.py`）
- [x] **P4** Q4 救援任务分区与资源配置优化 —— **23 个架次全部单点 ⇒ 15 个原子单元、0 个桥接架次**；
      **K=2 与 K=3 均可行且改动 0 架次**；资源 **28/29/30**，单组最大工作量 **11.27/6.28/5.62 h**
- [x] **P5** 论文撰写与图表定稿 —— 24 图 / 29 表 / 53 页 docx + PDF + 分目录备份
- [x] **P6** 论文排版整改 —— 公式 106 个（Word 原生对象，可双击编辑）/ 三线表 29 张 / 章页分页 / 图表同页
- [x] **P7** ★ Q2 建模缺陷修复（异构机队退化 / 首批专架次未铺开 / 派发准则退化）—— 见 ADR-029，结论由"首批时限不可行"翻转为"准时率 100%"（**旧求解器**）
- [x] **P8** LaTeX 工具链与论文格式转换 —— 装 MiKTeX 25.12 + pandoc 3.11；LaTeX→PDF 56 页/0 报错；三条 PDF→Word 路径实测（公式可编辑性见 `docs/PDF_TO_WORD.md`）；仓库仅保留 pdf2docx 一份论文产物
- [x] **P9** ★★ ADR-031 修复：Q3 中继「时间轴覆盖」缺陷 —— 三处缺陷全部修掉（服务区间被静默截短 / 排班不认服务窗口 / 校验器**从未执行**该项检查）。**该轮数值属旧管线，已被 P10 取代**（仅作决策轨迹保留）
- [x] **P10** ★★ **数值全量重建（2026-09-26，本轮唯一权威口径）** —— Q1 `exact_pack` 精确 DP、Q2 `dispatcher` CP-SAT、Q3 `timeline` 时间轴诊断、Q4 原子单元重算；新增 **ADR-033 ~ ADR-036**。详见下方「当前结果口径」
- [x] **P10b** ★★ **第二轮重跑（同日）** —— ① **Q4 中继归属修复**：`group_resources()` 旧判据要求"中继保障的运输架次全在本组"⇒ `R01-1`（覆盖 23 个架次）让 K=2/K=3 算出**中继 0 架 / 0 组**；新增 `assign_relays_to_groups()` 唯一归属 + 修掉 `load_q3()` 的一对一覆盖 ⇒ 资源 **28/29/30**、缺口重算；② **Q1 ρ 扫描改走精确 DP**（`sweep_reserve_ratio_exact`），启发式版本降为对照文件；③ 删除 25 架次旧管线的遗留表 `q3_中继时间覆盖复核.csv`。新增 **ADR-037 / ADR-038**

---

## ★ 当前结果口径（P10 之后，写论文务必按此）

| 问 | 关键指标 | 值 | 出处 |
|---|---|---|---|
| **Q1** | 架次数 / 机型构成 | **18**（**B×9 + C×9**） | `outputs/q1/metrics.json` |
| | 总能耗 / 累计作业时间 | **59.087498 kWh** / **32676.33 s** | 同上 |
| | 架次数下界 / gap | **18** / **0**（15/15 区逐区相等） | `q1_3_sortie_lower_bounds.csv` |
| | 对照（同组批改全 C 型） | 18 架次 / **74.2611 kWh** | `q1_3_strategy_comparison.csv` |
| | **ρ_g 敏感性（精确 DP 同口径）** | ρ=0.10 → **18 / 59.087498 / B:9+C:9**；ρ=0.20 → **同上（完全相同）**；ρ=0.30 → **20 / 67.180487 / B:9+C:11**；ρ=0.40 → 不可行（**2** 个区无可行组批）；ρ=0.50 → 不可行（**5** 个区） | `q1_4_rho_sweep.csv` |
| **Q2** | 架次数 / 机型构成 | **23**（A×6 + B×9 + C×8） | `outputs/q2/metrics.json` |
| | 总能耗 / 完工时间 | **66.789867 kWh** / **7843.2266 s（2.179 h）** | 同上 |
| | 时限达标 | 首批 **30/30**、期望 **80/80**（最紧期望余量 11.74 s） | 同上 |
| | 校验器 | **0 违规**（最大能耗偏差 0.003241 kWh、最大 SOC 偏差 0.002044） | `outputs/q2/feasibility.json` |
| | 最小返航 SOC | **0.230867**（BC02 执行 T15） | `q2_资源使用_电池.csv` |
| **Q3** | 运输 / 中继架次 | **23 + 3**（`R01-1` / `R02-1` / `R02-2`，2 架中继机） | `outputs/q3/metrics.json` |
| | 能耗 | 66.789867 + **4.306050** = **71.095918 kWh** | 同上 |
| | 联合完工 | **7939.2924 s**（运输完工 7843.2266 s） | 同上 |
| | 通信诊断 | **0.25 s** 采样 / **99889** 样本 / LOS 步长 60 m | 同上 |
| | 需中继 / 零中断架次 | **18 / 23** 需中继；**19 / 23** 零中断（`coverage_rate = 0.8261`） | 同上 |
| | 中断样本 | **928**（占全部样本 **0.929%**；平均水平中断占比 0.011037） | 同上 |
| | 中断集中架次 | T10(S013) **615**、T20(S009) **138**、T18(S005) **146**、T15(S004) **29** | `q3_直连状态诊断.csv` |
| | 最小链路裕量 | 直连 **−6.205 dB** / 中继 **+0.436 dB** | `outputs/q3/metrics.json` |
| | 中继最小返航 SOC | **0.278621** | 同上 |
| **Q4** | 原子单元 / 桥接架次 | **15**（每区独立）/ **0** | `outputs/q4/metrics.json` |
| | K=2 / K=3 可行性 | 均**可行**，改动 **0** 架次 | 同上 |
| | 资源总量 K=1/2/3 | **28 / 29 / 30（台·组）**（运输机 8/9/10、电池 15/15/15、中继机 2/2/2、中继组件 3/3/3） | `q4_方案对比.csv` |
| | 组间不均衡 / 单组最大工作量 | 0 / 0.2289 / 0.8425；**11.27 / 6.28 / 5.62 h** | 同上 |
| | 资源缺口（逐机型） | **K=1**：C 型电池 +1 组；**K=2**：B 型运输机 +1 架 且 C 型电池 +1 组；**K=3**：B 型运输机 +1 架、C 型运输机 +1 架 且 C 型电池 +1 组（中继机 / 中继组件不缺：2/2、3/6） | `q4_资源缺口.csv` |
| | ★ 中继归属口径 | `assign_relays_to_groups()` **唯一归属**（归给覆盖架次最多的组）⇒ K=2 的 G2、K=3 的 G2/G3 名下中继为 0，但实际仍依赖 G1 的中继 —— **乐观口径，必须与结论并列说明** | `q4_逐组明细.csv`、`q4_分区配置.csv`、ADR-037 |

> ## ⚠️ Q3 两条强制警示（必须与数值同时出现）
> 1. **巡航海拔是显式扩展模型**：3/4 中继方案取
>    `巡航海拔 = max(沿线 DEM 最高点 + 50 m, 悬停海拔)`；按附录 2 的严格读法，
>    相关结论属**有条件结论**。
> 2. **通信结论基于有限采样**：0.25 s 步长 + 60 m 视线步长，
>    **不是连续时间的数学证明**。

> ## ★ 三类信息（严禁混用）
> - **附件核对的输入**：机型/机队/电池/货箱/时限/DEM/链路预算；
> - **本文复现的结果**：上表全部数值（`outputs/summary.json` + `outputs/qN/metrics.json`）；
> - **仅作对照的候选结果**：给定方案的 **7740.19 s** 时刻表（**违反 S015 医疗箱 7200 s 硬时限**，
>   实际 7470.0 s 交付 ⇒ 不得写成"参考方案更快"）、`outputs/q3_4relay/`（4 中继；覆盖 18/23、
>   中断 2835 样本 ⇒ 更差）、`q2_机队对比.csv`（资源规模敏感性）、
>   `q1_4_rho_sweep_heuristic_ref.csv`（旧启发式 ρ 扫描，仅备查）。
>
> ★ **Q1 的正式 ρ 扫描（`q1_4_rho_sweep.csv`）现与答案路径同口径**（精确字典序 DP，`strategy = exact_dp`），
> **可以直接引用**；上面那个 `..._heuristic_ref.csv` 才是"仅供对照"的那一份（ADR-038）。

> ## ❌ 已作废的旧口径（不要再写进论文）
> | 旧值 | 现在 |
> |---|---|
> | Q1 18 架次「全部 C 型 / 75.07 kWh」 | **18 架次 B×9+C×9 / 59.087498 kWh** |
> | 「ρ 扫描是启发式数据、不可引用」（P10 首轮的说法） | **已作废**：正式扫描现为精确 DP（ρ=0.20 → 59.087498 kWh），启发式版本移入 `q1_4_rho_sweep_heuristic_ref.csv` |
> | 「ρ=0.20 扫描值 75.070591 kWh ≠ 答案值 59.087498 kWh」 | **不再成立**：两者现为同一个值（同口径扫描） |
> | Q2 25 架次（A8+B10+C7）/ 77.30 kWh / 3.02 h | **23 架次（A6+B9+C8）/ 66.789867 kWh / 7843.2266 s** |
> | Q3「几何覆盖 20/20 = 100%」 | **时间轴诊断：19/23 零中断、928 中断样本** |
> | Q3 时间轴覆盖 11/20、6 中继架次、79.65 kWh | **3 中继架次、71.095918 kWh、7939.2924 s** |
> | Q4 3 个原子单元 / 6 个桥接 / 资源 33-34-38 | **15 个原子单元 / 0 桥接 / 资源 28-29-30** |
> | Q4「资源 **28-28-30**」与「缺口 **B 型电池 +1**（三种分区都缺）」 | **作废**：那是中继归属 bug（K=2/K=3 的中继被记成 0 架）时期的中间值；现为 **28/29/30**，缺口为 **K=1 C 型电池 +1 / K=2 B 型机 +1 与 C 型电池 +1 / K=3 B 型机 +1、C 型机 +1 与 C 型电池 +1**。<br>⚠️ 注意：**K=3 缺 C 型运输机这一条现在仍然成立**（需求 3 / 库存 2），不要因为"B 型电池缺口消失"就把它一并删掉 |
> | `q3_中继时间覆盖复核.csv`（时间轴覆盖复核表） | **文件已删除**（25 架次旧管线遗留）；覆盖率只认 `q3_直连状态诊断.csv` + `q3_中继保障关系.csv` |
> | `outputs/qN/figures/` 下的原始图 | **目录已移除**；现行图在 `paper/figures/` 与 `paper/by_question/qN/figures/` |

---

## 已完成（摘要）

### 仓库与规范
- [x] 仓库结构、`.gitignore`、`.gitattributes`、`requirements.txt`
- [x] `scripts/commit.ps1` —— 一键提交并推送（含收工自检）
- [x] `scripts/check_env.py` / `run_all.ps1`
- [x] **`OPS_SPEC_D题.md` v2.1** —— 唯一操作规范（铁律 R1–R10、物理口径、四问验收清单）
- [x] `docs/legacy_D题/` —— 废弃 D 题方案归档（只读，含归档规范）
- [x] `docs/reference/` —— `参考文稿2.pdf`（★ 权威排版模板）+ 第三方同题解答（仅模板/交叉验证）

### 公共层（L0–L3，已被四问共用）
- [x] `common/`：`config.py`（D 题口径常量）、`io_utils.py`（结果溯源）、`metrics.py`
- [x] `q0_data/`：`discover.py`（附件清点）、`build_processed.py`（唯一附件解析器）
- [x] `geo/`：`crs.py`（局部切平面 + WGS84 曲率半径）、`dem.py`（`ElevationProvider` 协议）、`leg.py`
- [x] `physics/`：`payload.py`（`brentq` 反解最大安全载荷）、`energy.py`、`battery.py`（两阶段充电）、`leg_cache.py`
- [x] `comms/`：`link.py`（FSPL + 双向链路预算）、`los.py`（三维视线遮挡）、`service.py`（直连/中继/中断三态）
- [x] `verify/feasibility.py` —— **独立校验器**：不 import 任何 `qN_*`，从附件与 DEM 重算全部物理量，18 类约束 + 负样本测试

### 问题层（L4）
- [x] Q1 `q1_payload_grouping/`：载荷表（45 条 = 15 区 × 3 机型）/ ★ **`exact_pack.py` 精确字典序 DP 组批（答案路径）** / 下界（`sortie_lower_bound`）/ 启发式对照（FFD 等）/ ★ **ρ 敏感性同口径扫描（`sweep_reserve_ratio_exact` + `critical_reserve_ratios`）**
- [x] Q2 `q2_transport_schedule/`：★ **`solution.py::build_q23()` 方案装配（Q1 精确 DP 组批 + 4 区先行小架次 + 调用调度器）** / ★ **`dispatcher.py` CP-SAT 资源区间调度（可选区间 + NoOverlap + 两阶段充电占用 + 逐箱硬时限 + 可行解提示）** / 多点串飞模型（本轮 23 架次均为单点）/ 旧贪心派发与局搜（`schedule.py`/`solver.py`，仅置换对照）
- [x] Q3 `q3_comms_relay/`：★ **`timeline.py` 联合时间线诊断（0.25 s 采样逐时刻三态 + 两条链路最小裕量）** / 轨迹离散采样与悬停候选点（`coverage.py`）/ 中继架次几何与排班（`relay.py`）/ 中继保障关系（`relay_coverage.py`）
- [x] Q4 `q4_partitioning/`：图与连通分量（原子单元）/ 桥接架次 / ★ **中继唯一归属 `assign_relays_to_groups`（修掉"中继需求 0 架"的错误）** / RGS 枚举分区 / 资源核算（`group_resources(..., owned_relays=)`）

### 论文层
- [x] `src/report/make_figures.py` / `make_figures2.py` —— 24 图 / 33 个表文件（正文 29 张表） + **按问题分目录备份**
- [x] `src/report/equations.py` —— ★ 类 LaTeX → **OMML（Word 原生公式对象）**；含 MathType 转换链路（调研用，ADR-028）
- [x] `src/report/build_paper.py` —— ★ 论文装配（三线表 / 分页 / 图表同页 / 表头公式化）
- [x] 交付物：`paper/` 论文（Python 管线 **53 页 / 106 个可编辑公式**；另有 LaTeX 公式版 75 页、PDF 转 Word 71 页）、`论文体量报告.json`、图表清单与备份清单

### 测试
- [x] **251 passed**（物理公式逐条锁死 + 校验器负样本 + **硬约束闸门** + 四问端到端 + **ADR-031 中继窗口/区间集合 8 项新测试**）

---

## 环境（已验证，无需重装）

| 项 | 值 |
|---|---|
| Python | 3.13.7 (CPython) |
| 平台 | Windows 11 / AMD64 |
| CPU 核数 | 24 |
| 状态 | ✅ `status: ok` |

公式排版另需：Microsoft Office（提供 `OMML2MML.XSL`）、MathType 9+、Edge/Chrome（Chromium 内核）。

---

## 未决问题

### 仍待定

- [ ] `镇龙乡地理空间数据说明.pdf` 的正文内容尚未逐条提取（需确认是否还有额外口径约定）—— **OPEN-013**
- [ ] 论文页数 53 页（要求 50~100）；若删减内容会跌破下限，需重新核算
- [ ] ★ **Q3 新口径需写入论文/Q3 章**：把"**19/23 架次零中断 + 928 个中断样本**"作为结果、
      把"**2 架中继机的站位时间**"作为**资源限制**如实讨论；图表现仍可能按旧口径绘制，
      **必须重跑 `make_figures*` 与 `build_paper` 后复核**
- [ ] ★ **Q4 必须写明"中继唯一归属"是乐观口径**：`assign_relays_to_groups()` 把每个中继只归一组
      （`R01-1` 归给覆盖架次最多的组）⇒ K=2 的 G2、K=3 的 G2/G3 名下中继为 0，
      却仍依赖 G1 的中继保障，本核算**不额外补偿**。论文给缺口结论时必须并列声明（ADR-037）
- [ ] 论文中引用 `q3_运输架次.csv` 等表时**不得**再提旧文件 `q3_中继时间覆盖复核.csv`
      （该文件是 25 架次旧管线的遗留产物，**已删除**）
- [ ] ★ **给定方案时刻表的口径须在论文中说明**：7740.19 s 更短但**违反 S015 医疗箱 7200 s 硬时限**，
      须与 CP-SAT 的上界收紧探针结论一起呈现（`Cmax ≤ 7843.2 s` 对给定组批不可行、`≤ 7850 s` 可行）

### 已解决（P0 实测 + 后续）

- [x] 附件坐标 **WGS84 经纬度（EPSG:4326）**，必须先投影
- [x] DEM：**EPSG:4326 / 30 m / 无无效像元**；16 节点全部在范围内；**是 DSM 而非裸地 DEM**（ADR-019）
- [x] 8 架实体无人机机型分配：**A×4（U01–U04）、B×2（U05–U06）、C×2（U07–U08）**
- [x] 共享电池分机型总数：**A:6 / B:4 / C:4**；中继能源组件 **6 组**
- [x] 链路三条双向门限：**直连 122 dB / 中继接入 116 dB / 中继回传 126 dB**
- [x] 结果格式：**`结果提交模板.xlsx` 的 6 个 sheet**
- [x] 连续通信采样步长：现行实现取 **0.25 s**（`sample_dt_s`，99889 个样本），LOS 步长 60 m
- [x] ★★ **Q3 的覆盖率口径**（P10）：退役"几何可达覆盖"，改由 `timeline.py` 时间轴诊断直接给出
      "零中断架次 / 中断样本数 / 最小裕量"，缺口按资源限制如实上报（见 ADR-036）
- [x] ★★ **Q1 的下界口径**（P10）：`sortie_lower_bound()` 只在**能量可行的机型**上取最大载荷/体积
      （此前对全部机型取 max，会凭空造出 4 个"与下界的差距"，见 ADR-034）
- [x] ★★ **Q2 的组批内聚**（P10）：组批改进放在 `build_q23()` 内部，任何调用路径都得到同一组数（ADR-033）
- [x] ★★ **Q2 的最优性验证手段**（P10）：CP-SAT 可行解提示 + 独立的"逐档收紧 `Cmax` 上界"可行性探针（ADR-035）
- [x] ★★ **Q1 的 ρ 敏感性口径**（P10b）：`q1_4_rho_sweep.csv` 改由 `sweep_reserve_ratio_exact` 产出
      （精确字典序 DP 同口径，`strategy = exact_dp`），ρ=0.10 与 0.20 均为 18 架次 / 59.087498 kWh；
      启发式版本另存 `q1_4_rho_sweep_heuristic_ref.csv`（仅对照、不进论文，见 ADR-038）
- [x] ★★ **Q4 的中继归属口径**（P10b）：新增 `assign_relays_to_groups()`（唯一归属：覆盖架次最多的组），
      并修掉 `load_q3()` 把 47 条 pair 读成一对一字典的覆盖 bug ⇒ 资源 28/29/30、缺口重算（ADR-037）
- [x] ★★ **旧遗留文件清理**（P10b）：`outputs/q3/tables/q3_中继时间覆盖复核.csv`（25 架次旧管线产物）已删除；
      `outputs/qN/figures/` 旧目录已移除，图统一产出到 `paper/figures/` 与 `paper/by_question/`

---

## 交接备忘

- **★ P10b 之后第一件事**：论文与 `otheragent/` 里仍可能残留旧口径数字
  （Q1 全 C 型 75.07 kWh / Q2 25 架次 77.30 kWh / Q3 几何覆盖 100% 或 6 中继架次 79.65 kWh /
  **Q4 资源 28-28-30 与"B 型电池 +1"缺口**），必须按本文件「**当前结果口径**」表更新，并重跑：
  ```powershell
  python -m src.report.solve_solution          # ★ 一次跑完 Q1 精确 DP（含 ρ 扫描）+ Q2/Q3 装配 + 双中继对照 + 汇总（实测 135.3 s）
  python -m src.report.make_figures
  python -m src.report.make_figures2
  python -m src.report.build_paper
  python scripts\verify_paper.py
  python scripts\diag\check_paper_numbers.py   # 核对论文数字与 metrics
  ```
- 下一个 agent 应先执行：`git pull --rebase origin main`，然后**先读 `docs/AGENT_GUIDE.md`**（★ 四问的程序/图表/数据位置、答案与思维链、初稿使用须知都在那里），再读本文件与 `OPS_SPEC_D题.md`
- **铁律 R1：每次更改代码必须提交仓库**（改完即 commit，commit 即 push）
- 提交入口：`.\scripts\commit.ps1 -Scope <q0|q1|q2|q3|q4|phys|geo|comms|verify|spec|env|repo|report> -Type <feat|fix|data|model|exp|doc|repo|chore> -Message "..."`
- **物理公式先写测试**：改 `src/physics/`、`src/comms/` 的任何公式，必须同步补测试
- ★ **改动 Q3 的口径或结果后必须重跑 Q4**（Q4 继承 Q3 的架次划分，原子单元数会变；**不得硬编码**）
- ★ **Q3 数字必须带两条警示**（巡航海拔扩展模型 + 有限采样），它们已随 `Plan.caveats` 落盘，**不得删改**
- 被推翻的 D 题方案归档到 `docs/legacy_D题/`，说明**为何弃用**，不要就地删除
- ★ **改论文排版前先读 README「一·六 论文排版规范」**，尤其是 `w:tblPr` 顺序与
  `w:gridCol` 单位这两个坑（出错会让 Word 无法分页与导出 PDF）
- ★ **交付格式是硬约束**：`结果提交模板.xlsx` 的 6 个 sheet（题目正文未提及，极易漏）
- ★ **每个服务区恰好 2 个首批箱**（医疗 1 + 饮用水 1）→ 这两箱须同架次送达才能同时满足时限
