# D 题操作规范（OPS SPEC）
## 山区洪涝灾害下无人机运输与通信协同优化

> **文件定位**：本文件是 D 题全部建模工作的**唯一操作规范（single source of truth）**。
> 任何 agent（人或 AI）在动手之前必须先完整读完本文件，并严格遵守第 1 节的提交铁律。
>
> **远程仓库**：https://github.com/starfruitsz/MathAgent.git
> **仓库本地路径**：`C:\Users\28447\Desktop\数学建模\MathAgent`
> **规范版本**：v2.0（由 F 题切换而来；F 题遗留物归档至 `docs/legacy_F题/`）
> **适用赛题**：2026 年中国研究生数学建模竞赛 D 题

---

## 0. 铁律速查（TL;DR）

| # | 规则 | 违反后果 |
|:--:|---|---|
| **R1** | **每次更改代码必须提交仓库。** 改完即 `commit`，`commit` 后即 `push`。禁止累积多次改动后再一次性提交。 | 视为任务未完成 |
| **R2** | **本规范文件本身如有修改，也必须提交并推送至仓库。** | 视为任务未完成 |
| **R3** | 附件数据 `data/raw/**` **只读**，永不修改、永不删除。所有派生数据写入 `data/interim/` 或 `data/processed/`。 | 数据不可追溯，返工 |
| **R4** | **物理口径唯一**：附录 2/3 的时间、能耗、充电、链路公式**只实现一次**（`src/physics/`、`src/comms/`），所有问题共用。禁止各问各写一套。 | 四问结果口径不一致，直接失分 |
| **R5** | 每问**必产出** `outputs/qN/metrics.json` + `params.json` + 图 + 表 + **可行性校验报告**，缺一不可。 | 论文无据可写 |
| **R6** | 禁止把原始 DEM、xlsx、大中间产物提交进仓库（见第 1.4 节）。 | 仓库膨胀 |
| **R7** | 所有随机过程必须固定种子（`SEED = 42`），结果必须可复现。 | 结果不可信 |
| **R8** | **每个方案必须通过可行性校验器**才能写入结果文件。校验器是"验尸官"，不是装饰品。 | 提交不可行方案，全盘失分 |
| **R9** | 允许使用开源库/求解器，但**必须**在论文与 `docs/TOOLS.md` 中标注名称、版本号、核心参数。 | 学术规范扣分 |
| **R10** | 论文正文若使用 AI 工具，**必须文末披露**；不得直接复制 AI 生成的建模方案。 | **一经查实取消参评资格** |

---

## 1. 版本控制与提交规范（最高优先级）

### 1.1 铁律 R1：每次更改代码必须提交仓库

> ### ⚠️ 每次更改代码必须提交仓库
>
> **这是一条不可协商的硬性要求，适用于每一个 agent、每一次修改、每一个文件。**
>
> - 修改了任何一个 `.py` / `.md` / `.ipynb` / `.yaml` / `.txt` / `.json` 文件 → **立刻** `git add` + `git commit` + `git push`
> - 修好一个 bug → 立刻提交
> - 调整了一个超参数 → 立刻提交
> - 新增一个绘图函数 → 立刻提交
> - **禁止**"等我全部写完再一起提交"
> - **禁止**留下未提交的工作区改动过夜（或过一个任务回合）
> - 提交信息必须写清**改了什么、为什么改**
>
> 提交前自检（必须全部为真才能收工）：
> ```powershell
> git status --porcelain                # 输出必须为空
> git log origin/main..HEAD --oneline   # 输出必须为空（即已全部推送）
> ```

### 1.2 标准提交流程

```powershell
cd C:\Users\28447\Desktop\数学建模\MathAgent

# 一键脚本（推荐）
.\scripts\commit.ps1 -Scope q2 -Type model -Message "加入电池充电周转约束"

# 等价的手工流程
git add -A
git commit -m "model(q2): 加入电池充电周转约束"
git push origin main
```

`scripts/commit.ps1` 已实现（含收工自检与失败即抛异常），见第 6 节。

### 1.3 提交信息规范

格式：`<type>(<scope>): <简要说明>`

| type | 用途 | | scope | 对应内容 |
|---|---|---|---|---|
| `feat` | 新增功能/脚本 | | `q1`–`q4` | 四个问题 |
| `fix` | 修复 bug | | `q0` | 数据发现与公共层 |
| `data` | 数据处理与派生数据 | | `phys` | 飞行/能耗物理层 |
| `model` | 模型/数学形式变更 | | `geo` | DEM 与地理层 |
| `exp` | 实验、调参、结果产出 | | `comms` | 通信链路层 |
| `doc` | 文档、论文、规范更新 | | `spec` | 本规范文件 |
| `repo` | 仓库结构、依赖 | | `env` `repo` | 环境、仓库 |

示例：
- `feat(geo): 实现 DEM 沿线最高点采样与地形遮挡判定`
- `model(phys): 实现载荷-航程 3/2 次幂关系与最大安全载荷反解`
- `fix(comms): 修正双向链路门限应取两方向较小值`
- `exp(q2): 8 架异构无人机多点多架次调度求解`

### 1.4 提交边界

仓库**只**纳入：代码、文档、配置、小体量结果（`outputs/**` 中 < 5 MB 的 csv/json/md/png）。

`.gitignore` 已存在，关键排除项：

```gitignore
data/raw/**          # ★ 原始附件（DEM 可能很大）
data/interim/**      # 中间数据
*.tif *.tiff *.hgt   # DEM 栅格
*.pkl *.h5 *.parquet
*.zip *.7z
```

> `outputs/**` 的 `metrics.json` / `params.json` / `*.csv` / `*.png` 是**论文的证据链**，**必须入库**。

### 1.5 接手与交接协议

```powershell
cd C:\Users\28447\Desktop\数学建模\MathAgent
git pull --rebase origin main
Get-Content docs\PROGRESS.md -TotalCount 80      # 读进度与阻塞项
Select-String -Path docs\PROGRESS.md -Pattern '\[ \]'   # 未完成项
```

收工前必须更新 `docs/PROGRESS.md` 并提交（同样受 R1 约束）。

---

## 2. 环境与依赖

### 2.1 Python 环境

- **Python 3.13.7**（已实测通过，无需降级）
- 虚拟环境可选；当前全局环境已装好全部依赖

```powershell
cd C:\Users\28447\Desktop\数学建模\MathAgent
pip install -r requirements.txt
python scripts\check_env.py     # 期望 status: ok
```

### 2.2 D 题依赖栈（已全部安装并验证）

| 类别 | 包 | 用途 |
|---|---|---|
| 数值 | `numpy` `scipy` `pandas` `pyarrow` | 基础计算与数据 |
| **地理** | `rasterio` `geopandas` `shapely` `pyproj` | **★ DEM 读取、坐标投影、几何运算** |
| **地理** | `scikit-image` | 沿线采样（Bresenham）、栅格处理 |
| **优化** | `ortools` | **★ VRP/CP-SAT，问题二/三的路径与调度** |
| 优化 | `pymoo` `cvxpy` | 多目标进化、凸优化 |
| 图论 | `networkx` | 服务区图、依赖图、拓扑序 |
| 统计 | `statsmodels` `scikit-learn` `lmfit` `iminuit` | 敏感性分析、参数拟合与置信区间 |
| 可视化 | `matplotlib` `seaborn` `folium` | 静态图 + **交互式地图**（路线展示） |
| 因果/不确定 | `dowhy` `econml` `mapie` | （一般用不到，保留备选） |
| 工程 | `pyyaml` `tqdm` `joblib` `pytest` | 配置、进度、并行、测试 |

**关键版本**（完整清单见 `requirements.txt`，依赖树见 `requirements.lock.txt`）：

```
rasterio 1.5.1   geopandas 1.1.4   shapely 2.1.2   pyproj 3.8.0
ortools 9.15.6755   networkx 3.7   scikit-image 0.26.0   folium 0.20.0
numpy 2.4.4   scipy 1.17.1   pandas 3.0.2
```

> **已验证**：`PROJ 9.8.1` 可用，`EPSG:4326` 正常 —— geopandas/pyproj 可直接做投影变换，
> 这是**计算水平距离与 DEM 采样**的前提。

### 2.3 坐标系约定（★ 极易出错，务必统一）

| 用途 | 坐标系 | 说明 |
|---|---|---|
| 附件给定的坐标 | 待 `discover.py` 确认（很可能是 **WGS84 经纬度 EPSG:4326**） | 先按原样读入，不擅自变换 |
| **水平距离计算** | **投影坐标系**（UTM 或按 DEM 自带 CRS） | ★ 经纬度直接算欧氏距离是**错误的**，必须先投影 |
| DEM 采样 | 与 DEM 自身 CRS 一致 | `rasterio` 读出的 CRS 优先 |
| 高程基准 | **以附件/DEM 说明为准** | 注意 DEM 高程是"海拔"还是"椭球高" |

> **行动项**：`src/geo/crs.py` 必须提供 `to_metric_crs(gdf)`，
> 并**断言投影后单位是米**。所有 `d_ij` 必须来自投影坐标，且**写入日志备查**。

---

## 3. 仓库目录结构（必须遵守）

```
MathAgent/
├── README.md
├── OPS_SPEC_D题.md                  # ★ 本文件
├── requirements.txt / requirements.lock.txt
├── .gitignore / .gitattributes
│
├── data/
│   ├── raw/                         # ★ 只读★ 官方附件（不入库）
│   │   └── .gitkeep
│   ├── interim/                     # 清洗后中间数据（不入库）
│   └── processed/                   # 建模就绪派生数据（小文件可入库）
│       ├── nodes.csv                #   O01 + 15 服务区（含投影坐标、地面海拔）
│       ├── boxes.csv                #   80 个货箱（质量/体积/类型/时限）
│       ├── uav_types.csv            #   3 种运输机型参数
│       ├── relay_params.csv         #   中继无人机与能源组件参数
│       ├── link_params.csv          #   链路预算参数
│       └── leg_cache.parquet        #   ★ 航段预计算缓存（距离/爬升/时间/能耗）
│
├── src/
│   ├── common/
│   │   ├── config.py                # 路径、SEED、全局常量
│   │   ├── io_utils.py              # 统一读写 + 结果溯源
│   │   ├── metrics.py               # 评价指标
│   │   ├── plotting.py              # ⬜ 统一出图风格
│   │   └── registry.py              # ⬜ 运行记录
│   ├── q0_data/
│   │   ├── discover.py              # ✅ 已实现：清点附件、编号映射
│   │   ├── validate.py              # ⬜ 数据校验
│   │   └── build_processed.py       # ⬜ 生成 data/processed/ 建模接口
│   ├── physics/                     # ★ 附录2 的唯一实现（R4）
│   │   ├── leg.py                   # ⬜ 航段：水平距离/爬升/下降/巡航海拔
│   │   ├── energy.py                # ⬜ 航段能耗（水平 + 爬升附加）
│   │   ├── payload.py               # ⬜ 载荷-航程 3/2 关系 + 最大安全载荷反解
│   │   ├── battery.py               # ⬜ SOC、两阶段充电、周转可行性
│   │   └── precompute.py            # ⬜ 航段缓存生成
│   ├── geo/
│   │   ├── dem.py                   # ⬜ DEM 读取、双线性采样、沿线最高点
│   │   ├── los.py                   # ⬜ 地形遮挡判定（视线 vs 地形剖面）
│   │   ├── crs.py                   # ⬜ 坐标投影（★ 见 2.3 节）
│   │   └── grid.py                  # ⬜ 悬停候选点网格（问题三）
│   ├── comms/
│   │   ├── link.py                  # ⬜ FSPL、链路预算、双向门限、可用性
│   │   └── service.py               # ⬜ 直连/中继/中断三态判定
│   ├── q1_payload_grouping/         # ⬜ 问题一
│   ├── q2_transport_schedule/       # ⬜ 问题二
│   ├── q3_comms_relay/              # ⬜ 问题三
│   ├── q4_partitioning/             # ⬜ 问题四
│   ├── verify/
│   │   └── feasibility.py           # ★ 统一可行性校验器（R8）
│   └── report/                      # ⬜ 论文表格与图
│
├── outputs/
│   ├── env_report.json              # ✅
│   ├── data_inventory.json          # ✅
│   └── q1 ... q4/                   # metrics/params/tables/figures/校验报告
│
├── scripts/
│   ├── commit.ps1                   # ✅ 一键提交推送
│   ├── check_env.py                 # ✅ 环境自检
│   ├── run_all.ps1                  # ⬜ 端到端复现
│   └── tools/                       # 从 GitHub 拉取的外部工具
│
├── docs/
│   ├── PROGRESS.md                  # ★ 进度看板
│   ├── DATA_NOTES.md                # ★ 附件字段结构实测记录
│   ├── MODEL_NOTES.md               # ★ 各问数学形式与推导
│   ├── TOOLS.md                     # 外部工具来源/版本/许可证
│   ├── DECISIONS.md                 # 决策记录（ADR）
│   ├── PAPER_OUTLINE.md             # ⬜ 论文骨架与图表清单
│   └── legacy_F题/                  # F 题归档（只读，不再维护）
│
└── tests/
    ├── test_q0_discover.py          # ✅ 已实现
    ├── test_common.py               # ✅ 已实现
    ├── test_physics.py              # ⬜ ★ 公式级单元测试（必须）
    ├── test_geo.py                  # ⬜
    ├── test_comms.py                # ⬜
    └── test_verify.py               # ⬜
```

---

## 4. 数据协议

### 4.1 数据现实（开工前必读）

> **当前工作区 `C:\Users\28447\Desktop\数学建模` 内只有 6 份题目 Word 文档，没有任何附件数据。**
> D 题附件**必须先从竞赛官方渠道下载**，放入 `data/raw/`。

**因此 `src/q0_data/discover.py` 是所有工作的第一步，且是阻塞性前置任务。**
（该脚本已实现并通过测试，无数据时会以 exit 1 正确报阻塞。）

### 4.2 附件清单（附录 1 明确给出，共 5 + 1 项）

**基础参数数据**（位于 `数据/无人机应急物资运输基础数据/`）：

| # | 文件名 | 提供内容 | 主要消费者 |
|:--:|---|---|---|
| 1 | `调度中心与服务区.xlsx` | 临时调度中心 O01 + 15 个服务区的基本信息（**坐标、地面海拔、作业高度**） | 全部 |
| 2 | `物资需求与配送时限.xlsx` | 各服务区物资需求、**货箱属性**、配送时间要求、**逐箱货箱信息**（80 箱） | Q1 Q2 Q3 Q4 |
| 3 | `运输无人机数据.xlsx` | **3 种机型参数** + **共享电池参数**（含等效完全充电时间 `T_full`） | Q1 Q2 Q3 Q4 |
| 4 | `中继无人机数据.xlsx` | 中继无人机 + 能源组件参数（含悬停离地高度上限、准备/建链/周转时间、`T_full`） | Q3 Q4 |
| 5 | `通信链路参数.xlsx` | 固定网关 G01、运输无人机、中继无人机的**链路预算参数**（发射功率、天线增益、灵敏度、衰落裕量、系统损耗、遮挡附加损耗 `L_obs`、载波频率 `f`） | Q3 |

**地理空间与 DEM 数据**（位于 `数据/镇龙乡地理空间数据/`）：

| # | 内容 | 说明 |
|:--:|---|---|
| 6 | **30 米 DEM** | 关键栅格输入 |
| 7 | `镇龙乡地理空间数据说明.docx` | ★ **必须精读**，含 CRS、高程基准、DEM 覆盖范围 |

### 4.3 `discover.py` 必须回答的问题（P0 验收）

- [ ] 5 个 xlsx 的**真实文件名**与**每个 sheet 的列名**（中文列名要逐一登记）
- [ ] `调度中心与服务区.xlsx`：坐标是经纬度还是投影坐标？**单位是什么**？
- [ ] 服务区是否有**编号 Si（i=1..15）**列，与题目 "Si" 的对应关系
- [ ] `物资需求与配送时限.xlsx`：80 箱的**箱号**、所属服务区、**质量 kg**、**体积**、**物资类型**（医疗/首批保障/其他）、**期望送达时间**、**首批截止时间** 的确切列名
- [ ] `运输无人机数据.xlsx`：**3 种机型的型号代码**、`L_g0`（空载标准航程 m）、`L_gF`（满载标准航程 m）、`Q_g`（最大载货质量 kg）、**最大装载体积**、`v↑ / v_c / v↓`、`E_g^use`（单组电池可用能量 kWh）、`ρ_g`（返航安全余量比例）、**共享电池总数**、`T_full`
- [ ] **8 架实体无人机如何在 3 种机型间分配**（题目说"共配置 8 架"，需确认分配表）
- [ ] `中继无人机数据.xlsx`：悬停离地高度上限、水平巡航功率、悬停功率、通信附加功率、`T_full`、能源组件总数
- [ ] `通信链路参数.xlsx`：`P_t`、`G_t`、`G_r`、`P_sens`、`M`（衰落裕量）、`L_sys`、`L_obs`、`f`（MHz），以及**各主体（G01 / 运输机 / 中继机）的取值差异**
- [ ] DEM：**CRS**、分辨率、`nodata`、覆盖范围、高程单位
- [ ] 把以上全部回填到 `docs/DATA_NOTES.md`

### 4.4 数据处理五原则

1. **只读原则**：`data/raw/**` 永不写入。派生数据落 `data/interim/` 或 `data/processed/`。
2. **可追溯原则**：每个派生文件在 `outputs/data_inventory.json` 登记 `源文件 + 脚本 + git commit + 时间戳`。
3. **单位统一原则**：★ **内部一律用 SI**（m、s、kg、kWh、W、Hz→MHz 明确标注）。
   附件里的"km""分钟"等必须在读入时立即换算，并在 `DATA_NOTES.md` 记录换算。
4. **编号统一原则**：`O01` 与 `S1..S15` 的内部标识固定；货箱标识用附件原箱号，不做重编号。
5. **预计算原则**：所有航段的 `(d_ij, h⁺_ij, h⁻_ij, t_ij, E_ij)` **只算一次**，缓存到
   `data/processed/leg_cache.parquet`。问题二/三的求解器只查表，不重复调 DEM。

---

## 5. 物理与通信口径（★ 全题最易失分处）

> **铁律 R4**：以下公式**只实现一次**（`src/physics/`、`src/comms/`）。
> 四个问题必须共用同一套函数。任何一处口径分叉，都会导致四问结果互相矛盾。

### 5.1 航段几何（附录 2）

**巡航海拔**：
```
H_cruise(i,j) = max{ DEM(p) : p 位于 i→j 的水平直线段上 } + 50 m
```
★ 注意是**该航段所经过 DEM 像元的最高地面高程**，不是两端点的高程最大值。

**作业高度**：

| 节点 | 作业高度 |
|---|---|
| `O01`（调度中心） | 取其**地面海拔** |
| `S_i`（服务区） | 取**地面海拔 + 30 m** |

**连续访问多个服务区**：每次投送后**均从 30 m 作业高度重新爬升**。

**爬升/下降高度**：
```
h⁺_ij = H_cruise(i,j) − H_op(i)      （爬升高度）
h⁻_ij = H_cruise(i,j) − H_op(j)      （下降高度）
d_ij  = 水平巡航距离（投影平面上的直线距离，单位 m）
```

### 5.2 航段飞行时间

```
t_gij = h⁺_ij / v_g↑  +  d_ij / v_g^c  +  h⁻_ij / v_g↓
```

### 5.3 载荷–航程关系（★ 3/2 次幂，别写错）

```
L_g(q) = L_g0 − (L_g0 − L_gF) · (q / Q_g)^(3/2),    0 ≤ q ≤ Q_g
```

性质（**必须写进单元测试**）：
- `L_g(0) = L_g0`（空载标准航程）
- `L_g(Q_g) = L_gF`（满载标准航程）
- `L_g(q)` 关于 `q` **单调不增**且**凸**（因为指数 3/2 > 1）

### 5.4 最大安全载荷（问题一核心，需**反解**）

已给往返水平距离 `d` 时，"最大安全载荷" `q_max` 由**能量约束取等号**定义：
```
E_g^T(q_max; d) = (1 − ρ_g) · E_g^use
```
其中 `E_g^T` 是**往返航段能耗之和**（去程载荷 q、回程载荷 0，见 5.5）。

> ★ **关键提醒**：`E_g^T` 对 `q` 是**非线性的**（经由 `L_g(q)`），
> 因此 `q_max` **一般不能解析求出**，需用**单调性 + 二分法（brentq）**数值反解。
> 求解后**必须回代验证** `|E_g^T(q_max) − (1−ρ_g)E_g^use| < tol`。

**注意区分两个约束**：
- **能量约束** → 给出 `q_max`
- **载质量上限 `Q_g`** → 硬上限
- **装载体积上限** → 硬上限
- 最终最大安全载荷 = `min(能量反解值, Q_g)`

### 5.5 航段能耗

```
E_gij(q) = E_gij^hor(q) + E_gij^up(q)
```
- 水平巡航能耗 `E^hor`：与载荷相关（经等效航程 / 巡航功率）
- 爬升附加能耗 `E^up`：按爬升高度与爬升功率计算
- **下降能耗效率取 0** → **不单独计算下降附加能耗**（★ 别多加一项）
- 质量取**该航段上的实际剩余载荷** `q_pij`（随投送递减）

**架次能耗与返航安全余量**：
```
E_p^T = Σ_{(i,j) ∈ p} E_gij( q_pij )  ≤  (1 − ρ_g) · E_g^use
```

### 5.6 中继无人机时间与能耗

- 飞行时间：同样按**爬升 / 巡航 / 下降**三段计算
- 水平巡航能耗：**巡航功率 × 巡航时间**
- 爬升附加能耗：按 5.5 的相同关系，**质量取计划起飞总质量**
- **通信服务能耗** = （悬停功率 + 通信附加功率）× 服务时长
- 往返悬停位置的地形净空：按 5.1 的航段规则确定
- 需满足**返航电量要求**
- 固定准备时间、建链时间、架次周转时间：见 `中继无人机数据.xlsx`

### 5.7 两阶段等效充电模型（★ 分段函数，极易写错）

```
                ⎧ T_full · [ 0.65 · (0.90 − s) / 0.90 + 0.35 ],   0 ≤ s < 0.90
t_chg(s) =      ⎨
                ⎩ T_full · 0.35 · (1 − s) / 0.10,                0.90 ≤ s ≤ 1
```

**自检点（必须写进测试）**：
- `s = 0` → `t_chg = T_full · (0.65 + 0.35) = T_full` ✓
- `s = 0.90` → 上段 `T_full·0.35`，下段 `T_full·0.35·0.10/0.10 = T_full·0.35` ✓ **两段连续**
- `s = 1` → `0` ✓

**资源占用规则**：
- 运输共享电池与中继能源组件都是**独立资源**，各自记录 SOC
- 初始 SOC = **100%**
- 任务结束剩余 SOC 由可用能量与实际能耗算出，**不得低于返航电量下限**
- **同一资源的任务占用与充电时段不得重叠**；不同资源可并行充电
- 同一机型的共享电池可在该机型不同实体无人机间调度；**不同机型不可混用**
- 附件给出的共享电池总数**已包含**实体无人机初始使用与备用周转所需电池

### 5.8 通信链路（附录 3）

**(1) 地形遮挡**：由两端点三维位置与 **30 m DEM** 判断视线连线是否被地形遮挡
```
b_ijt = 0  无遮挡
b_ijt = 1  有遮挡
```

**(2) 接收门限与双向链路预算**
```
P_th,b = P_sens,b + M_b

L_max,a→b = P_t,a + G_t,a + G_r,b − L_sys − P_th,b

L_max,a↔b = min( L_max,a→b , L_max,b→a )        ← ★ 必须取较小值
```
> ★ 双向判定是硬要求（运输控制与状态回传都要保障），**不能只算单向**。

**(3) 传播损耗**
```
L_FSPL,ijt = 32.45 + 20·log10(f) + 20·log10(D_ijt)
```
- `f` 单位 **MHz**，`D_ijt` 单位 **km** ← ★ 单位陷阱！内部用 m，代入前必须 /1000
- `D_ijt` 是**三维直线距离**（含高度差）

```
L_path,ijt = L_FSPL,ijt + L_obs · b_ijt
```

**(4) 单链路可用性**
```
A_ijt = 1  若 L_path,ijt ≤ L_max,i↔j
A_ijt = 0  否则
```

**(5) 通信服务状态（三态判定）**
```
直连状态：A(运输机 ↔ G01) = 1
中继状态：直连不可用，且 A(运输机 ↔ 中继机) = 1 且 A(中继机 ↔ G01) = 1（★ 同一时刻同时可用）
通信中断：其余情况
```
- **不允许中继无人机之间多跳转发**
- 每架运输无人机在任一时刻**只能由 G01 或一架中继无人机**提供保障

### 5.9 连续通信约束（问题三的关键）

> 运输无人机在**爬升、巡航、下降及物资投送**阶段**均应保持连续通信**。

这意味着 Q3 的校验必须**沿整条航段轨迹逐时刻采样**判定通信状态，而不是只查两端点。
**推荐做法**：每条航段按固定时间步（如 1 s）或固定距离步离散，逐点判定三态，
要求**全程无"通信中断"**。采样步长须做**敏感性分析**并在论文中说明。

---

## 6. 四问工作流（输入 / 输出 / 验收）

> **通用要求**：每问产出
> `outputs/qN/metrics.json`、`params.json`、`tables/*.csv`、`figures/*.png+*.pdf`、
> `run_log.json`（git commit + 时间 + 耗时 + 种子）、**`feasibility.json`（R8 校验报告）**。

### 6.1 Q0：数据发现与公共层（阻塞性前置，最先做）

**输入**：`data/raw/**` → **输出**：`outputs/data_inventory.json`、`docs/DATA_NOTES.md`、`data/processed/*.csv`

**验收**：第 4.3 节清单全部勾选 + `leg_cache.parquet` 生成成功。

---

### 6.2 问题一：单点往返运输能力与货箱组批方案

**输入**：节点坐标、30 m DEM、机型参数、80 箱数据
**输出**：`outputs/q1/**`

**任务拆解**：

| 子任务 | 要求 | 方法建议 |
|---|---|---|
| 1a 最大安全载荷 | 3 机型 × 15 服务区的**单点往返**最大安全载荷 | 能量约束反解（二分）+ 与 `Q_g`、体积上限取 min |
| 1b 货箱组批 | 货箱**不可拆分**、每箱**只能安排一次**，满足**载质量 / 装载体积 / 返航安全能量余量** | **装箱问题**（bin packing）；按服务区独立求解 |
| 1c 组批优化 | 综合**往返架次数 / 总运输能耗 / 累计作业时间**，说明**权衡或优先关系** | 多目标；给出 Pareto 或加权方案 + 权衡曲线 |
| 1d 敏感性 | **返航安全余量 ρ_g 变化**对最大安全载荷与组批结果的影响 | 扫 ρ_g，画"ρ_g — 最大载荷 — 架次数"曲线 |

**建模要点**：
- 每个架次形如 `O01 → S_i → O01`，**仅服务一个服务区**，**不得跨服务区组批**
- 同一服务区**可由多个架次分批服务**
- 载荷随航段递减：去程载货、回程空载（★ 回程能耗按空载算）
- 组批是**一维/二维装箱**：质量约束 + 体积约束

**验收**：
- [ ] 3×15 的最大安全载荷表完整，且**每个值都回代验证**了能量约束紧
- [ ] 说明"最大安全载荷"与 `Q_g`、体积上限何者起约束作用
- [ ] 组批方案覆盖**全部 80 箱且无重复**
- [ ] 三个指标（架次/能耗/时间）的权衡关系有**定量**说明
- [ ] ρ_g 敏感性分析有图有表

---

### 6.3 问题二：异构无人机多点多架次运输调度

**输入**：Q1 成果 + 无人机/电池/充电周转参数 + 时限
**输出**：`outputs/q2/**`

**每个架次可访问一个或多个服务区**，完成后返回 O01。

**决策变量（联合确定）**：
1. 货箱组批
2. **服务区访问顺序**
3. **运输机型**
4. **具体执行无人机**（8 架实体机）
5. **共享电池**分配
6. **各架次开始时刻**

**约束**：
- 货箱不可拆分
- 载质量与装载体积
- 返航安全余量 `ρ_g`
- **无人机与共享电池数量**
- **充电周转**（5.7 节，同资源占用与充电不重叠）
- **物资时限**：医疗物资满足**期望送达时间**；**首批保障货箱**满足**首批截止时间**

**目标（多目标）**：配送及时性 + 全部任务完成时间 + 运输能耗 + 架次数
> **全部任务完成时间** = 所有运输无人机完成最后一个架次并返回 O01 的**最晚时刻**

**验收**：
- [ ] 给出**运输路线、架次安排、逐箱送达时刻、无人机与电池资源使用情况**
- [ ] **检验方案的资源可行性**（R8 校验器全绿）
- [ ] 各指标权衡关系有定量说明
- [ ] 明确区分"医疗物资期望送达"与"首批保障截止时间"两类时限的处理

---

### 6.4 问题三：通信约束下的运输与中继联合调度方案

**输入**：Q2 成果 + 通信链路参数 + 中继无人机参数 + DEM
**输出**：`outputs/q3/**`

**新增决策变量**：
- 中继无人机的**悬停位置**（三维：平面位置 + 飞行海拔）
- **服务时段**
- **能源组件分配**

**约束**：
- 货箱时限、载荷与能量、资源可用性（同 Q2）
- **★ 连续通信**：运输无人机在爬升、巡航、下降、投送**全程**保持通信（5.9 节）
- 中继悬停位置**位于 DEM 覆盖范围内**
- 悬停**离地高度不超过附件规定上限**
- 中继**不允许多跳**；每架运输机同时刻只由 G01 或一架中继保障

**目标（多目标）**：物资配送及时性 + **联合任务完成时间** + 运输与中继**总能耗** + **两类无人机使用架次**
> **联合任务完成时间** = 所有运输无人机**和中继无人机**完成最后一个架次并返回 O01 的**最晚时刻**

**验收**：
- [ ] 给出**运输与中继联合调度方案**
- [ ] 说明**货箱交付、资源使用、通信保障**三类情况
- [ ] **连续通信逐时刻校验**通过（含采样步长敏感性）
- [ ] 报告"直连 / 中继 / 中断"三态的时间占比

---

### 6.5 问题四：救援任务分区与资源配置优化方案

**输入**：**Q3 的联合调度方案**（★ 必须以此为基）
**输出**：`outputs/q4/**`

**分区规则（硬约束，违反即无效）**：
- 15 个服务区划分成 **2 组** 和 **3 组** 两种方案（分区方案由参赛者确定）
- 每个服务区**必须且只能**属于一个任务组；**每组至少一个服务区**
- ★ **保持 Q3 已确定的货箱组批、服务区访问顺序、运输与中继任务安排、通信保障关系不变**
- ★ **若同一运输架次同时涉及多个服务区，则这些服务区应划入同一任务组**
- 各任务组**仅承担本组**服务区的运输与通信保障任务
- **任务执行期间各类资源不得跨组调配**

**核算内容**：每组独立执行所需的
`运输无人机数量` + `共享电池数量` + `中继无人机数量` + `中继能源组件数量`

**任务拆解**：

| 子任务 | 要求 |
|---|---|
| 4a 分区 | 给出 **2 组**与 **3 组**两种分区方案 |
| 4b 资源配置 | 分别核算四类资源的**组内需求** |
| 4c 对比 | 从**资源配置规模 / 资源冗余 / 组间工作量均衡 / 与现有库存的资源缺口**四方面比较两种分区 |
| 4d 缺口 | 若某方案所需资源**超过现有库存**，给出**资源缺口及其原因** |

**建模要点**：
- 分区必须**服从 Q3 的架次划分**：先把架次聚类，再把服务区归组
  （因为"同架次的多服务区必须同组"）
- 这是一个**带约束的聚类/划分问题**（可枚举：15 个服务区的划分数有限，2 组与 3 组规模可控）
- **资源冗余** = 现有库存 − 组内需求；**缺口** = 负冗余
- **组间均衡**：用各组工作量（架次数/架次时长/能耗）的方差或最大最小比度量

**验收**：
- [ ] 2 组与 3 组分区方案均满足全部硬约束
- [ ] 四类资源的需求与冗余逐组列出
- [ ] 与**现有库存**对比，明确给出缺口
- [ ] 组间工作量均衡有**定量指标**
- [ ] 明确说明"以 Q3 方案为基础、保持架次划分不变"的继承关系

---

## 7. 代码质量与复现规范

### 7.1 编码约定

- **统一入口**：`src/qN_*/run_qN.py`，接受 `--config configs/qN.yaml` 与 `--seed`
- **统一配置**：超参、路径、常量写 `configs/*.yaml`，**禁止硬编码魔法数字**
- **统一随机源**：`SEED = 42`（`src/common/config.py`）
- **统一日志**：`io_utils.get_logger('qN')` → 控制台 + `outputs/qN/run.log`
- **禁止 notebook 作为主流程**：`.ipynb` 只用于探索
- **核心数学过程写成纯函数**，便于 `pytest` 断言

### 7.2 结果记录模板（`outputs/qN/metrics.json`）

```json
{
  "question": "q2",
  "git_commit": "<脚本自动写入>",
  "timestamp": "2026-01-01T12:00:00+08:00",
  "seed": 42,
  "runtime_sec": 123.4,
  "data_sources": ["调度中心与服务区.xlsx", "物资需求与配送时限.xlsx"],
  "metrics": {
    "n_sorties": 0, "total_energy_kwh": 0.0, "makespan_s": 0.0,
    "on_time_rate": 0.0, "n_uav_used": 0, "n_battery_used": 0
  },
  "checks": {"feasible": true, "violations": []}
}
```

### 7.3 测试要求（★ 物理公式必须逐条锁死）

`pytest tests/ -v` 必须全绿。**必测项**：

| 测试对象 | 断言 |
|---|---|
| `L_g(q)` | `L_g(0)=L_g0`；`L_g(Q_g)=L_gF`；单调不增；凸性 |
| `q_max` 反解 | 回代后能量约束**紧**（相对误差 < 1e-6）；`q_max ≤ Q_g` |
| `t_gij` | 三段之和；`h⁺=h⁻=0` 时退化为纯巡航 `d/v_c` |
| `E_gij` | **下降不单独计能耗**；爬升高度为 0 时爬升附加能耗为 0 |
| `t_chg(s)` | `s=0 → T_full`；`s=0.9` **两段连续**；`s=1 → 0`；关于 s 递减 |
| DEM 采样 | 已知小栅格上的**最高点**与**双线性插值**结果正确 |
| 地形遮挡 | 构造"山挡住视线"与"视线越过山顶"两个用例 |
| FSPL | `L_FSPL = 32.45 + 20log10(f) + 20log10(D)`，**单位 km/MHz** |
| 双向门限 | `L_max,a↔b = min(两个方向)`，方向不对称时取小 |
| 通信三态 | 直连 > 中继 > 中断的**优先级**；中继需两段**同时**可用 |
| 单纯形（继承） | 见 `tests/test_common.py` |
| 可复现性 | 同 seed 两次运行结果 `np.allclose` |
| **可行性校验器** | 对**故意构造的违规方案**必须报错（负样本测试） |

> ★ **负样本测试是硬要求**：校验器必须能抓出"超载""超能量""超时限""充电时段重叠"等违规。
> 只测通过用例的校验器等于没有校验器。

### 7.4 一键复现

```powershell
cd C:\Users\28447\Desktop\数学建模\MathAgent
python scripts\check_env.py
python -m src.q0_data.discover
python -m src.q0_data.build_processed
python -m src.physics.precompute          # 生成航段缓存
python -m src.q1_payload_grouping.run_q1
python -m src.q2_transport_schedule.run_q2
python -m src.q3_comms_relay.run_q3
python -m src.q4_partitioning.run_q4
python -m src.report.make_figures
python -m pytest tests\ -v
.\scripts\commit.ps1 -Scope repo -Type exp -Message "端到端复现通过"
```

---

## 8. 进度看板（`docs/PROGRESS.md` 维护要求）

每次收工必须更新，至少包含：
- 阶段状态（`- [ ]` / `- [x]`）
- 当前 HEAD commit
- **阻塞项**（需人工介入的）
- 未决问题
- 交接备忘（下一个 agent 先做什么）

---

## 9. 工具与依赖决策记录（ADR 摘要）

| # | 决策 | 理由 | 影响 |
|:--:|---|---|---|
| ADR-001 | 地理计算用 `rasterio`+`geopandas`+`pyproj`，不引入 `richdem`/`elevation` | 后两者在 Python 3.13 上安装风险高、且 `elevation` 需联网下载 SRTM（违规风险）；附件已直接给 DEM | 地理层 |
| ADR-002 | 优化用 `ortools` + 自研启发式，不引入 `vrpy` | `vrpy` 依赖重、版本兼容风险；本题约束高度定制（充电周转、连续通信），通用 VRP 库收益有限 | Q2 Q3 |
| ADR-003 | **所有航段量预计算并缓存**到 `leg_cache.parquet` | DEM 采样是热点；Q2/Q3 求解器会反复查询同一航段 | 性能 |
| ADR-004 | 最大安全载荷用**单调性 + brentq 反解**，不用解析近似 | `L_g(q)` 的 3/2 次幂使能量对 q 非线性，无闭式反函数 | Q1 |
| ADR-005 | 内部**一律 SI 单位**，进出附件时换算 | FSPL 要 km/MHz，航程要 m，混用是单位错误的根源 | 全部 |
| ADR-006 | 可行性校验器**独立于求解器**，且必须有**负样本测试** | 求解器与校验器同源会"自证清白"；负样本测试保证校验器真的能抓错 | R8 |
| ADR-007 | 连续通信判定**沿航段逐时刻采样**，不用端点近似 | 题目明确要求"爬升、巡航、下降及投送阶段均应保持连续通信" | Q3 |
| ADR-008 | 分区先**聚类架次**再归组服务区 | "同架次的多服务区必须同组"是硬约束，先分服务区会导致方案不可行 | Q4 |
| ADR-009 | 提交铁律由 `scripts/commit.ps1` 强制（含收工自检） | 人工易忘；脚本在未推送时抛异常 | R1 |
| ADR-010 | F 题遗留物归档至 `docs/legacy_F题/`，不删除 | 保留决策历史；避免误用 F 题结论 | 仓库卫生 |

---

## 10. 论文写作与提交要求

### 10.1 论文必答清单

**通用**：问题假设、**符号定义**、模型与算法、数据处理过程、主要结果及其**适用条件**、
★ **各问题之间的数据继承关系**、公共物理规则/单位/对象编号/**计算口径保持一致**。

**Q1**：3 机型 × 15 服务区最大安全载荷表 / 组批方案 / 三指标权衡 / **ρ_g 敏感性**
**Q2**：路线 + 架次安排 + **逐箱送达时刻** + 无人机与电池资源使用 / 资源可行性检验 / 指标权衡
**Q3**：运输与中继联合调度方案 / 货箱交付 + 资源使用 + **通信保障情况** / 连续通信校验 / 指标权衡
**Q4**：2 组与 3 组分区方案 / 四类资源配置与冗余 / **与现有库存的资源缺口及原因** / 组间均衡

**图表要求**（题目明确）：图表应能反映
**运输路线、调度时序、资源占用、通信保障、任务分区** 等关键结果。

**提交物**：论文 + 按附件模板的结果文件、**检查说明**、**可运行程序**。

### 10.2 学术规范红线（违反即取消资格）

1. 不得引入**其他数据集**替换或补充赛题附件
2. 不得以 AI 输出作为学术依据；公式与结论须引用**正式发表文献或可核验数据**
3. 不得直接复制 AI 生成的建模方案（查重雷同即违规）
4. 不得隐瞒 AI 使用情况 → **论文末尾附录必须披露**所用 AI 工具、输入、输出处理策略、
   开发框架、开源软件、技术路线、假设条件与超参数
5. 提交材料中严禁出现参赛单位、队员姓名、队伍编号等身份信息

---

## 11. Agent 工作循环（每次任务的标准动作）

```
┌─ 1. 接手 ────────────────────────────────────────────┐
│  git pull --rebase origin main                        │
│  读 docs/PROGRESS.md → 确认阶段、阻塞项、未决问题        │
│  读本文件对应章节 + docs/legacy_F题/（如需历史决策）      │
└──────────────────────────────────────────────────────┘
                        ↓
┌─ 2. 检查前置 ────────────────────────────────────────┐
│  data/raw/ 有数据吗？没有 → 完成 P0 或报告阻塞           │
│  docs/DECISIONS.md 是否有未决问题拦截本次任务？          │
│  改物理公式？→ 先改 tests/test_physics.py（测试先行）    │
└──────────────────────────────────────────────────────┘
                        ↓
┌─ 3. 小步实施 ────────────────────────────────────────┐
│  一次只做一件事（一个函数 / 一个模型 / 一张图）           │
│  写完立即本地验证：pytest tests/ -k <相关>              │
│  涉及物理口径 → 必须同步更新 tests/test_physics.py      │
└──────────────────────────────────────────────────────┘
                        ↓
┌─ 4. 提交（★ 铁律 R1，不可跳过）★ ─────────────────────┐
│  .\scripts\commit.ps1 -Scope qN -Type <type> -Message "..." │
│  自检：git status --porcelain          必须为空         │
│        git log origin/main..HEAD       必须为空         │
└──────────────────────────────────────────────────────┘
                        ↓
┌─ 5. 更新看板 ────────────────────────────────────────┐
│  更新 docs/PROGRESS.md                                │
│  更新 docs/TOOLS.md（若引入新工具）                     │
│  更新 docs/DECISIONS.md（若做了技术决策）                │
│  再次 commit（同样受 R1 约束）                          │
└──────────────────────────────────────────────────────┘
```

---

## 12. 快速命令备忘

```powershell
cd C:\Users\28447\Desktop\数学建模\MathAgent

git pull --rebase origin main

python scripts\check_env.py
python -m src.q0_data.discover
python -m src.q0_data.build_processed
python -m src.physics.precompute
python -m src.q1_payload_grouping.run_q1
python -m src.q2_transport_schedule.run_q2
python -m src.q3_comms_relay.run_q3
python -m src.q4_partitioning.run_q4

pytest tests\ -v

# ★ 提交（每次改动必做）
.\scripts\commit.ps1 -Scope q2 -Type model -Message "说明"

# 收工自检
git status --porcelain
git log origin/main..HEAD --oneline
```

---

## 附录 A：本规范的变更记录

| 版本 | 日期 | 变更 | 提交 |
|---|---|---|---|
| v1.0 | — | 初版（F 题：算力约束下 LLM 资源配置） | `024f632` |
| **v2.0** | 切换至 D 题 | 全面改写为「山区洪涝灾害下无人机运输与通信协同优化」；新增物理口径章（第 5 节）、可行性校验器要求（R8）、坐标系约定（2.3 节）、工具链改为地理+运筹栈；F 题遗留物归档至 `docs/legacy_F题/` | 本次提交 |

> 变更本文件后，必须同步更新上表并**提交推送**（铁律 R2）。

---

## 附录 B：本规范强制条款索引

| 条款 | 内容 | 出处 |
|---|---|---|
| **R1** | **每次更改代码必须提交仓库，改完即提交、提交即推送** | 第 1.1 节 |
| **R2** | **本 md 文件修改后必须上传至仓库** | 第 1.1 节 |
| R3 | `data/raw/**` 只读 | 第 4.4 节 |
| **R4** | **物理口径唯一实现，四问共用** | 第 5 节 |
| R5 | 每问必产出 metrics/params/图/表/校验报告 | 第 6 节 |
| R6 | 大文件不入库 | 第 1.4 节 |
| R7 | 固定随机种子 | 第 7.1 节 |
| **R8** | **方案必须通过独立可行性校验器** | 第 7.3 节 |
| R9 | 工具/求解器须标注版本与参数 | 第 9 节 |
| R10 | AI 使用必须披露 | 第 10.2 节 |

---

> **再次强调：每次更改代码必须提交仓库。**
> 这不是建议，是硬性操作要求。任何未提交的改动都视为未完成的工作。
