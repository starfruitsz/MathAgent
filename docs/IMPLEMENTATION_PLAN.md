# 实现计划（IMPLEMENTATION PLAN）— 物理层与地理层

> 本文件是 P0b / P1 的实现蓝图。**分层解耦是本计划的第一原则**：
> 每层只依赖其下一层，且**下层不得反向依赖上层**。
> 任何跨层调用都必须经由明确的接口（Protocol / 纯函数）。

---

## 1. 为什么要分层解耦（本题的具体痛点）

| 痛点 | 若不接耦会怎样 |
|---|---|
| DEM 采样慢（1486×1309 栅格） | 每算一次能耗都读栅格 → Q2/Q3 求解器跑不动 |
| 经纬度坐标 | 各处自己写投影代码 → 单位混乱、结论互相矛盾 |
| 公式口径必须四处一致（铁律 R4） | 各问各写一份 → 四问结果对不上，直接失分 |
| 单元测试需要**不依赖真实数据** | 耦合后测试必须带 7.5 MB DEM 才能跑 |

**解耦收益**：物理公式可用**解析高程函数**测试（不碰 DEM），
DEM 的真实读取只在 `geo/` 层发生一次并被缓存。

---

## 2. 分层结构（严格单向依赖）

```
┌─────────────────────────────────────────────────────────────┐
│ L4  问题层   q1_payload_grouping / q2 / q3 / q4             │
│     只做"决策"，不碰公式；全部物理量向 L3 索要                  │
└───────────────────────────┬─────────────────────────────────┘
                            │ 只依赖 ↓
┌───────────────────────────▼─────────────────────────────────┐
│ L3  领域层   physics/  comms/  verify/                       │
│     physics: 载荷-航程、能耗、电池、架次汇总                    │
│     comms:   链路预算、三态判定                                 │
│     verify:  独立可行性校验器（不许调用 L4 的求解器）             │
└───────────────────────────┬─────────────────────────────────┘
                            │ 只依赖 ↓（关键解耦点）
┌───────────────────────────▼─────────────────────────────────┐
│ L2  几何层   geo/leg.py                                      │
│     把 (节点, 高程) 变成 (d, h+, h-, cruise_alt)               │
│     ★ 通过 ElevationProvider 协议拿高程 —— 不直接读 DEM         │
└───────────────────────────┬─────────────────────────────────┘
                            │ 只依赖 ↓
┌───────────────────────────▼─────────────────────────────────┐
│ L1  基础层   geo/crs.py   geo/dem.py                         │
│     crs: 经纬度 → 米制投影（唯一入口，ADR-011）                  │
│     dem: 栅格读取 + 双线性采样 + 沿线最高点；实现 Provider      │
└───────────────────────────┬─────────────────────────────────┘
                            │ 只依赖 ↓
┌───────────────────────────▼─────────────────────────────────┐
│ L0  数据/常量层   common/config.py   q0_data/                 │
│     路径、SEED、题目常量、附件解析与建模接口                      │
└─────────────────────────────────────────────────────────────┘
```

**★ 关键解耦点：`ElevationProvider` 协议**

```python
class ElevationProvider(Protocol):
    def elevation_at(self, lon: float, lat: float) -> float:
        """返回该经纬度处的地面海拔（m），越界抛 OutOfBoundsError。"""
```

- `geo/dem.py` 提供一个**基于 rasterio 的真实实现** `RasterElevationProvider`
- `tests/` 提供**解析实现** `AnalyticElevationProvider`（如 `z = 100 + 0.001*x`）
- 于是 `physics/` 与 `geo/leg.py` 都能**脱离 DEM 做单元测试**

---

## 3. 模块清单与职责边界

### L0 · 数据/常量层（已完成）
| 模块 | 状态 | 职责 |
|---|---|---|
| `common/config.py` | ✅ | 路径、SEED、题目常量、容差 |
| `common/io_utils.py` | ✅ | 统一读写 + 溯源 |
| `common/metrics.py` | ✅ | 指标 |
| `q0_data/discover.py` | ✅ | 附件清点 |

### L1 · 基础层（本次实现）
| 模块 | 职责 | 关键约束 |
|---|---|---|
| `geo/crs.py` | 经纬度 → 米制投影的**唯一入口** | 断言输出单位是米；缓存投影器 |
| `geo/dem.py` | 栅格读取、双线性采样、**沿线最高点**、Provider 实现 | 只读；越界显式报错 |

### L2 · 几何层（本次实现）
| 模块 | 职责 | 关键约束 |
|---|---|---|
| `geo/leg.py` | 由节点+Provider 算出航段的 `(d, h⁺, h⁻, H_cruise)` | **不读 DEM**，只调 Provider |

### L3 · 领域层（本次实现）
| 模块 | 职责 | 关键约束 |
|---|---|---|
| `physics/payload.py` | `L_g(q)` 载荷-航程；`q_max` 反解 | ★ 3/2 次幂；brentq 反解 + 回代验证 |
| `physics/energy.py` | 航段能耗、爬升附加、架次能耗汇总 | ★ **下降不单独计能耗** |
| `physics/battery.py` | SOC、两阶段充电、周转可行性 | ★ 分段函数在 s=0.9 处连续 |
| `physics/leg_cache.py` | 批量预计算 + Parquet 缓存 | 缓存键 = (i, j, provider_id) |
| `comms/link.py` | FSPL、链路预算、双向门限、可用性 | ★ 单位 km/MHz；双向取小 |
| `comms/service.py` | 直连/中继/中断三态判定 | ★ 中继需两段同时可用 |
| `verify/feasibility.py` | 独立校验器 | ★ 必须有负样本测试 |

### 尚未实现（后续）
`q0_data/build_processed.py`、`q1`–`q4`、`report/`、`common/plotting.py`

---

## 4. 实现顺序（测试先行）

每步都**先写测试再写实现**，且每步单独提交（铁律 R1）。

| 步 | 内容 | 测试文件 |
|:--:|---|---|
| 1 | `physics/payload.py` + `physics/battery.py` + `physics/energy.py` | `tests/test_physics.py` |
| 2 | `geo/crs.py` | `tests/test_geo.py` |
| 3 | `geo/dem.py`（含 Provider 协议与解析实现） | `tests/test_geo.py` |
| 4 | `geo/leg.py` | `tests/test_geo.py` |
| 5 | `comms/link.py` + `comms/service.py` | `tests/test_comms.py` |
| 6 | `verify/feasibility.py`（含负样本） | `tests/test_verify.py` |

> 先做步 1：物理层是**纯函数**、无外部依赖、最容易测，且是 Q1–Q3 的共同基础。

---

## 5. 必须锁死的公式（来自附件实测）

| 项 | 公式/值 | 来源 |
|---|---|---|
| 载荷-航程 | `L_g(q) = L_g0 − (L_g0−L_gF)·(q/Q_g)^{3/2}` | 题目附录 2 |
| 巡航海拔 | 航段经过 DEM 像元**最高地面高程 + 50 m** | 题目附录 2 |
| 作业高度 | O01 = 地面海拔；服务区 = 地面海拔 + 30 m | 题目附录 2 |
| 航段时间 | `h⁺/v↑ + d/v_c + h⁻/v↓` | 题目附录 2 |
| 航段能耗 | 水平巡航 + 爬升附加；**下降效率 = 0** | 附件确认 |
| 返航余量 | `E_p^T ≤ (1 − 0.20)·E_g^use` | 附件 `ρ_g = 20%` |
| 充电 | `s<0.9`: `T_full[0.65(0.9−s)/0.9 + 0.35]`；`s≥0.9`: `T_full·0.35(1−s)/0.1` | 题目附录 2 |
| FSPL | `32.45 + 20log₁₀f(MHz) + 20log₁₀D(km)` | 题目附录 3 |
| 双向门限 | `min(L_max,a→b, L_max,b→a)` | 题目附录 3 |
| 天线增益 | `G_t = G_r = G`（附件只给一列） | ADR-018 |

---

## 变更记录

| 日期 | 变更 | 提交 |
|---|---|---|
| v1.0 | 建立实现计划：五层结构、ElevationProvider 解耦点、模块职责、实现顺序 | 本次提交 |
