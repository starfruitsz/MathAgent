# 数据说明摘录与编号映射（DATA_NOTES）

> 本文件在 **P0 阶段**由 `src/q0_data/discover.py` 的输出填充。
> 当前为骨架 —— **尚未获取竞赛官方附件，内容待补**。

---

## 1. 数据现实

| 项 | 状态 |
|---|---|
| `data/raw/real_attachments/` | **空**（官方附件未下载） |
| 《数据说明》文件 | **缺失** |
| 附件编号 ↔ 实际文件名对照 | **未知** |

> 题目原文声明："若清单与实际磁盘文件不一致，**以实际文件为准**。"
> 因此 `discover.py` 的实测结果具有最高优先级。

---

## 2. 已知的编号用途约束（来自题目正文，可直接使用）

| 编号 | 用途 | 硬性约束 |
|---|---|---|
| A1–A3 | 质量信号数据（A1 抽样集，A2/A3 扩展集） | 质量评价**必须使用全量记录**；须给出域级 Q 并与 A1 抽样集对照 |
| A4–A15 | 配方实验数据（17 域配比 ↔ 交叉熵损失） | A6–A11 为**检验集**；A12–A15 为**外推表**，配比表与 Loss 表**须成对使用** |
| A16 | 跨体系域分类参考映射 | 质量体系与配比体系域分类不同，须说明关联规则与假设 |
| A18 | 质量评分验算（可选用） | — |
| B1–B5 | 经典标度律数据 | B1 主拟合；B2 或 B3 之一做模型族外/插值轨迹验证；B4 与 B5 做跨族或文献验证 |
| B6–B8 | **半合成补充集** | 可用于补充分析，**不得表述为直接实验观测**，须说明局限 |
| B9–B10 | 百亿参数以上外推讨论 | 须标注可信度边界 |
| B11–B12 | 辅助数据 | — |
| C1–C2 | 模型评测数据 | Q4 须使用 C1 或 C2 |
| C3 | 模型评测数据 | Q4 须使用 |
| C4 | 模型元数据 | 含算力、数据量、开源权重等字段，Q4 须使用 |
| C5–C6 | **Loss–Benchmark 桥接数据** | 须按**可比性等级**区分使用 |
| C7 | **上下文长度 L_ctx 可行取值依据** | ★ Q3 中 L_ctx **外生给定**，可行取值**必须依据 C7** |
| C8 | 逐任务评测明细 | Q4 须做**至少一项逐任务聚合分析**，不得仅用汇总表 |
| C10 | 待确认 | 待 `discover.py` 确认 |

---

## 3. 实测清单（待 P0 填充）

### 3.1 文件清点

| # | 相对路径 | 大小 | 扩展名 | 推测编号 | 备注 |
|---|---|---|---|---|---|
| — | 待填充 | | | | |

### 3.2 编号 ↔ 实际文件名对照

| 编号 | 实际文件名 | 是否与题目清单一致 | 差异说明 |
|---|---|---|---|
| — | 待填充 | | |

### 3.3 字段结构

| 文件 | 字段名 | dtype | 形状 | 缺失率 | 取值范围 | 说明 |
|---|---|---|---|---|---|---|
| — | 待填充 | | | | | |

---

## 4. 关键确认项（P0 必须回答）

- [ ] **C7 的真实内容**是什么？`L_ctx` 的可行取值集合具体有哪些？
- [ ] **C5/C6 的"可比性等级"**字段叫什么？分级规则是什么？
- [ ] **17 个训练领域**的完整名录（The Pile 子领域）
- [ ] **22 个质量指标**的完整名录、字段类型、**方向说明**（哪些是负向？）
- [ ] 哪些质量指标是**多维列表型**？压缩为标量的推荐方式？
- [ ] A4–A15 中，配比表与 Loss 表的具体列名与配对键
- [ ] A12–A15 外推表的配比范围（是否超出训练单纯形支撑？）
- [ ] C4 中"算力""数据量""开源权重"字段的确切列名
- [ ] C8 逐任务明细的表结构（任务名、得分、模型、时间）

---

## 5. 数据处理规则

### 5.1 方向统一

22 个质量指标全部转为"越高越好"。负向指标做补变换 `x' = 1 − MinMax(x)`。
**被翻转的指标必须逐一列明**（待 P0 后填充）：

| 指标名 | 原始方向 | 变换方式 | 依据 |
|---|---|---|---|
| 待填充 | | | |

### 5.2 多维列表压缩

| 指标名 | 原始类型 | 压缩方式 | 敏感性检验结果 |
|---|---|---|---|
| 待填充 | | | |

### 5.3 可信度标注

| 数据源 | 可信度等级 | 使用限制 |
|---|---|---|
| B6–B8 | 半合成（基于真实数据校准） | 可用于补充分析，**不得表述为直接实验观测** |
| B9–B10 | 估算/外推 | 讨论百亿参数以上外推时须标注边界 |

---

## 变更记录

| 日期 | 变更 | 提交 |
|---|---|---|
| 初始 | 建立骨架，录入题目正文中已知的编号用途约束 | `<commit>` |
